import themes.sbss

#img_favorite: filename=subview_btn_favorite_deselected.png, width=50dp, height=50dp
/cell: display=block, position=absolute, gravity=center, y=-30dp, \
        font-family=$SANSSERIF_MEDIUM, text-color=$THEME_COLOR_00, text-align=center

if $SCREEN_WIDTH > "5"
    /cell: font-size=1.3
else
    /cell: font-size=1.2
end
